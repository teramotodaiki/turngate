interface:
  display_name: "Turngate"
  short_description: "Enable opt-in repository gates for local agents"
  default_prompt: "Use $turngate to explain enablement and check whether the current user request or repository instructions explicitly opt in; do not run Turngate otherwise."
