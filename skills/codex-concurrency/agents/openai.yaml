interface:
  display_name: "Codex Concurrency"
  short_description: "Share repository gates across local coding agents"
  default_prompt: "Use $codex-concurrency before modifying this repository so the active turn owns the required gate."
